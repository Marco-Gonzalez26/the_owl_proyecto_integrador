
export default function CheckoutError({session}) {
    console.log('🛒 CheckoutError');
    console.log('🛒 CheckoutError session:', session);
    return (
        <div>
            <h1>Checkout Error</h1>
        </div>
    );
}
